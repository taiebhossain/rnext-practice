import { createContext } from 'react';

const MovieContext = createContext(null);
const ThemeContext = createContext(null);
const DarkContext = createContext(null);

export { DarkContext, MovieContext, ThemeContext };
